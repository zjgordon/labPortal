# Lab Portal Smoke Test Configuration
# Copy this file to .env and modify as needed

# Portal base URL
BASE_URL=http://localhost:3000

# Admin credentials (for reference, not used in API key auth)
ADMIN_EMAIL=admin@local
ADMIN_PASSWORD=admin123

# API Key for smoke test authentication
# This should match the value in your .env.local file
ADMIN_API_KEY=smoke-test-key

# Note: The smoke test script uses a hardcoded API key "smoke-test-key"
# To use a custom API key, modify the script or set ADMIN_API_KEY in your .env.local
